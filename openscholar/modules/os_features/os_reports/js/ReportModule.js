(function () {
  var reportModule = angular.module('ReportModule', []);
  reportModule.controller('SiteReport', function ($scope) {
    $scope.update = function(query) {
      // update results table
    };
  });
})();
